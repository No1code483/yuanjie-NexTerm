/**
 * 样式常量定义
 * 注意：此文件已被重构，所有样式常量已迁移到 CSS 变量系统中
 * 请使用 theme.css 中的 CSS 变量，例如：var(--nt-green)
 * 
 * 迁移说明：
 * - COLORS 中的颜色值 → 使用 theme.css 中的 CSS 变量
 * - SIZES 中的尺寸值 → 使用 theme.css 中的 CSS 变量
 * - VARIANTS 中的样式变体 → 使用 CSS 模块和 CSS 变量
 * - 其他常量 → 根据具体需求迁移或删除
 * 
 * 示例：
 * 原代码：COLORS.green
 * 新代码：var(--nt-green)
 * 
 * 原代码：SIZES.medium.padding
 * 新代码：var(--nt-button-padding-medium)
 */

export {}